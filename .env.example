# Server-only configuration. Copy to .env.local for local testing; never commit values.
# Use the deployed Google Apps Script Web app URL ending in /exec, not /dev.
GOOGLE_SHEETS_WEBHOOK_URL=
# Copy WEBHOOK_SECRET from Apps Script > Project Settings > Script properties.
GOOGLE_SHEETS_WEBHOOK_SECRET=
