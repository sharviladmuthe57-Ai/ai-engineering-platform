"use client";
import { Menu, X } from "lucide-react";import { useEffect,useState } from "react";import { mailto,siteConfig } from "@/config/site";
export function Navigation(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const on=()=>setScrolled(scrollY>24);on();addEventListener("scroll",on,{passive:true});return()=>removeEventListener("scroll",on)},[]);return <header className={`nav ${scrolled?"scrolled":""}`}><a href="#top" className="wordmark" aria-label={`${siteConfig.companyName} home`}><span className="mark"/>{siteConfig.companyName}</a><nav className={open?"navLinks open":"navLinks"} aria-label="Primary">{siteConfig.navigation.map(x=><a key={x.href} href={x.href} onClick={()=>setOpen(false)}>{x.label}</a>)}<a className="button navCta" href={mailto}>Talk to us</a></nav><button className="menuButton" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button></header>}
